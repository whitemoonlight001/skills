# MacroDroid 微信通话自动录音配置指南

## 📱 功能概述

本配置将实现：
- ✅ 微信通话开始时自动开始录音
- ✅ 微信通话结束时自动停止录音
- ✅ 录音文件自动保存到指定目录

---

## ⚠️ 前置准备

### 1. 必要权限

确保 MacroDroid 已获得以下权限：
- [ ] 通知访问权限
- [ ] 录音权限
- [ ] 存储权限
- [ ] 无障碍服务（可选但推荐）
- [ ] 后台运行权限

### 2. 微信设置

确保微信通话时会显示通知（通常默认开启）。

---

## 🔧 配置方案一：基于通知触发器（推荐）

### 宏 1：微信通话开始 - 启动录音

#### 触发器
```
类型：通知收到
应用：微信 (WeChat)
文本过滤：包含关键词（如"通话中"、"正在通话"、"语音通话"、"视频通话"）
可选：启用正则表达式匹配
```

#### 动作
```
动作 1：启动录音
  - 音频源：语音通话（Voice Call）或麦克风（Microphone）
  - 输出格式：MP3 或 M4A
  - 保存路径：/sdcard/Music/WeChat Calls/
  - 文件名格式：WeChatCall_{year}{month}{day}_{hour}{minute}{second}

动作 2：显示通知（可选，用于确认）
  - 标题：微信通话录音
  - 内容：录音已开始

动作 3：设置变量（用于状态跟踪）
  - 变量名：IsWeChatCalling
  - 值：true
```

#### 约束
```
约束 1：变量 IsWeChatCalling = false（防止重复触发）
```

---

### 宏 2：微信通话结束 - 停止录音

#### 触发器
```
类型：通知清除
应用：微信 (WeChat)
文本过滤：包含"通话中"等关键词

或者

类型：通知收到
应用：微信 (WeChat)
文本过滤：包含"通话结束"、"通话已结束"等关键词
```

#### 动作
```
动作 1：停止录音

动作 2：显示通知（可选）
  - 标题：微信通话录音
  - 内容：录音已保存

动作 3：设置变量
  - 变量名：IsWeChatCalling
  - 值：false
```

#### 约束
```
约束 1：变量 IsWeChatCalling = true（仅在通话中触发）
```

---

## 🔧 配置方案二：基于应用状态 + 轮询

### 宏 1：微信通话检测（定时器）

#### 触发器
```
类型：定期间隔
  - 间隔：2-5 秒
  - 使用闹钟：是（确保精确触发）
```

#### 动作
```
动作 1：Shell 脚本/应用状态检测
  - 检测微信是否在前台且处于通话状态
  - 或者检测音频焦点/通话状态

动作 2：条件判断
  - 如果检测到通话开始且未在录音 → 启动录音
  - 如果检测到通话结束且正在录音 → 停止录音
```

---

## 🔧 配置方案三：混合方案（最可靠）

### 宏 1：启动录音（主方案）

#### 触发器
```
触发器 1：通知收到（微信，包含通话关键词）
触发器 2：应用启动/前台（微信，可选备用）
```

#### 动作
```
动作 1：条件：变量 IsRecording = false
动作 2：启动录音（配置见方案一）
动作 3：设置变量 IsRecording = true
动作 4：设置变量 CallStartTime = [当前时间]
动作 5：显示通知
```

---

### 宏 2：停止录音

#### 触发器
```
触发器 1：通知清除（微信，通话通知）
触发器 2：通知收到（微信，通话结束通知）
触发器 3：应用关闭/后台（微信，可选备用）
触发器 4：定期检查（每5秒，用于保险）
```

#### 动作
```
动作 1：条件：变量 IsRecording = true
动作 2：停止录音
动作 3：设置变量 IsRecording = false
动作 4：计算并保存录音时长（可选）
动作 5：显示通知
```

---

## 📝 详细配置步骤

### 步骤 1：创建变量

1. 打开 MacroDroid
2. 进入"变量"（Variables）
3. 添加以下变量：
   - `IsRecording`（布尔型，初始值 false）
   - `CallStartTime`（日期/时间型，可选）
   - `RecordingFilePath`（文本型，可选）

---

### 步骤 2：配置"开始录音"宏

1. 点击"+"添加新宏
2. 命名为：微信通话 - 开始录音
3. **添加触发器**：
   - 选择"通知收到"
   - 应用：选择微信（WeChat）
   - 文本过滤：选择"包含"
   - 输入关键词：`通话中`、`正在通话`、`语音通话`、`视频通话`
     - 建议添加多个触发器，每个关键词一个
   - 可选：启用"忽略大小写"
4. **添加动作**：
   - 条件判断：如果 `IsRecording` = `false`
   - 录音控制 → 启动录音
     - 音频源：根据需要选择（语音通话/麦克风）
     - 格式：MP3 或 M4A
     - 目录：选择保存路径（如 `/sdcard/Music/WeChatCalls/`）
     - 文件名：可以使用宏文本，如 `WeChat_{yyyyMMdd_HHmmss}`
   - 变量设置 → `IsRecording` = `true`
   - 通知显示（可选）
5. **添加约束**（可选）：
   - 如果需要，可以添加时间限制等

---

### 步骤 3：配置"停止录音"宏

1. 点击"+"添加新宏
2. 命名为：微信通话 - 停止录音
3. **添加触发器**：
   - 触发器 1：通知清除
     - 应用：微信
     - 文本过滤：通话相关关键词
   - 触发器 2：通知收到
     - 应用：微信
     - 文本过滤：`通话结束`、`已结束`
   - 触发器 3：定期间隔（作为保险）
     - 间隔：5-10 秒
4. **添加动作**：
   - 条件判断：如果 `IsRecording` = `true`
   - 录音控制 → 停止录音
   - 变量设置 → `IsRecording` = `false`
   - 通知显示（可选）

---

## 🎛️ 录音配置建议

### 音频源选择

- **语音通话（Voice Call）**：直接录制通话音频（推荐）
- **麦克风（Microphone）**：录制麦克风输入（包括你的声音和环境音）
- **语音下行（Voice Downlink）**：仅录制对方声音
- **语音上行（Voice Uplink）**：仅录制你的声音
- **语音通话（Voice Communication）**：综合录制（推荐）

### 输出格式

- **MP3**：兼容性好，文件较小
- **M4A/AAC**：质量更好，文件稍大
- **WAV**：无损但文件大

### 保存路径

建议：`/sdcard/Music/WeChat Calls/` 或 `/sdcard/Recordings/WeChat/`

---

## ⚠️ 注意事项

### 法律合规

⚠️ **重要提示**：
- 在大多数地区，未经对方同意录制通话可能违反法律
- 请确保遵守您所在地区的法律法规
- 建议在通话开始前告知对方正在录音
- 某些国家/地区要求双方同意

### 实际测试

1. 不同版本的微信通知文本可能不同
2. 建议先测试微信通话时的通知内容
3. 可以使用"显示通知"动作来调试
4. 可以先手动触发几次测试录音是否正常

### 权限问题

- 如果录音不工作，检查权限是否全部授予
- 在 Android 10+ 上，后台录音可能有限制
- 建议将 MacroDroid 设置为"不优化电池使用"

---

## 🔍 调试技巧

### 方法 1：通知调试

在宏中添加"显示通知"动作，在关键步骤显示当前状态。

### 方法 2：日志记录

使用"写入文件"动作记录调试信息到文本文件。

### 方法 3：查看实际通知

先进行一次微信通话，观察通知栏的实际显示内容，然后调整关键词。

---

## 📋 完整关键词列表（建议添加多个）

### 通话开始关键词
- 通话中
- 正在通话
- 语音通话
- 视频通话
- 连接中
- 等待接听

### 通话结束关键词
- 通话结束
- 已结束
- 通话已结束
- 通话时长

---

## 💡 进阶功能（可选）

### 功能 1：自动整理录音文件

添加宏在录音停止后：
- 移动文件到按日期分类的文件夹
- 重命名文件包含更多信息

### 功能 2：云备份

录音完成后自动上传到云盘（需要额外配置）

### 功能 3：录音质量调整

根据需要调整录音采样率和比特率

---

## 🆘 常见问题

### Q: 录音没有启动？
A: 检查：
- 通知关键词是否正确
- MacroDroid 是否有通知访问权限
- 录音权限是否授予
- 微信版本的通知格式

### Q: 录音文件在哪里？
A: 查看你在"启动录音"动作中设置的保存路径

### Q: 只录到了我的声音，没录到对方？
A: 尝试更换音频源，选择"语音通话"或"语音通信"

### Q: 通话结束后录音没有停止？
A: 检查通知清除触发器是否正确配置，可以添加定期检查作为备用

---

*配置完成后，建议先进行测试通话，确保一切正常工作！*
